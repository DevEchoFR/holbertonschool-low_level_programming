#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string to stdout
 * @str: pointer to the string
 *
 * Return: void
 */

void _puts(char *str)
{
	int n = 0;
    while (s[n] != '\0') n++;
    return n;
}
