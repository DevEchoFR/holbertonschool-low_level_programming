#include <stdio.h>
/**
 * main - Prints the lowercase alphabet followed by a newline
 * Return: Always 0 (Success)
 */
int main(void)
{
for (int i = 0; i <= 9; i++)
putchar('0' + i);
putchar('\n');
return 0;
}
